#include <iostream>
#include <vector>

class Solution {
public:
  std::vector<int> getRow(int rowIndex) {
    std::vector<std::vector<int>> paTri;
    std::vector<int> first{1};
    paTri.insert(first);
    int i = 1;
    while (i <= rowIndex){
      std::vector<int> new_row;
      for (int j = 1; j < paTri.size(); j++){
        
      }
      paTri.insert()
    }
}
};

int main(){
  return 0;
}